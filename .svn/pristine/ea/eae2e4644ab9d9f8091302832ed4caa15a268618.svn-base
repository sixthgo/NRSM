
// i18n(internationalization) resource initialize
var i18n_resources = i18n_resources || {
    dev: {},
    'ko': {},
    'en': {},
    'zh': {}
};

// set default validator
/*
jQuery.validator.setDefaults({
    errorElement: "span",            // error 발생시 span 으로 error 메세지 표기
    highlight: function (element, errorClass, validClass) {               // 에러 표시 하이라이트 설정
        $(element).parents('.control-group').removeClass('success');
        $(element).parents('.control-group').addClass('error');
    },
    onkeyup: false,                // keyup event 사용 안함
    onclick: false,                  // onclick event 사용 안함
    onfocusout: false,               // focusout event 사용 안함
    onselect: false,                 // onselect event 사용 안함
    onselectstart: false,            // onselectstart event 사용 안함
    showErrors: function (errorMap, errorList) {                          // 에러를 보여주는 방법
        if (this.numberOfInvalids()) {
            $.wf.alert(errorList[0].message);
        }
    },
    unhighlight: function (element, errorClass, validClass) {              // 하이라이트 해제.
        $(element).parents('.control-group').removeClass('error');
        $(element).parents('.control-group').addClass('success');
    }
});*/